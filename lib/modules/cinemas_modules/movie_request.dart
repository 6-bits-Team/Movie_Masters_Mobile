import 'package:flutter/material.dart';
class MovieRequest extends StatelessWidget {
  const MovieRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}