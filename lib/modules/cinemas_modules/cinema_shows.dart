import 'package:flutter/material.dart';
class CinemaShows extends StatelessWidget {
  const CinemaShows({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}