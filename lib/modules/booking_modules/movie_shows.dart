import 'package:flutter/material.dart';
class MovieShows extends StatelessWidget {
  const MovieShows({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}