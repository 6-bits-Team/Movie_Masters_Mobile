import 'package:flutter/material.dart';
class CinemaRequests extends StatelessWidget {
  const CinemaRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}